<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
>

    <data>

        <import type="com.example.yoshida_makoto.kotlintest.ui.viewmodel.UserPlayListRowViewModel"/>
        <import type="android.view.View"/>

        <variable
            name="vm"
            type="UserPlayListRowViewModel"/>
    </data>

    <RelativeLayout
        android:id="@+id/vm.music_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClickListener="@{vm.clickListener}"
        android:orientation="vertical"
        android:background="@drawable/ripple"
        android:padding="@dimen/small_dip2">

        <TextView
            android:id="@+id/music_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{vm.playList.title}"
            android:textSize="@dimen/big_text"
            tools:text="text"/>

    </RelativeLayout>
</layout>